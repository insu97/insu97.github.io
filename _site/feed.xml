<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-03-28T20:45:52+09:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">INSU’s Blog</title><subtitle>데이터 사이언스와 머신러닝에 관한 블로그</subtitle><author><name>INSU</name></author><entry><title type="html">Book Notes App - 개인 전용 학습 노트 뷰어 + RAG 검색 PWA</title><link href="http://localhost:4000/2026/03/28/book-notes-app.html" rel="alternate" type="text/html" title="Book Notes App - 개인 전용 학습 노트 뷰어 + RAG 검색 PWA" /><published>2026-03-28T00:00:00+09:00</published><updated>2026-03-28T00:00:00+09:00</updated><id>http://localhost:4000/2026/03/28/book-notes-app</id><content type="html" xml:base="http://localhost:4000/2026/03/28/book-notes-app.html"><![CDATA[<h1 id="book-notes-app---개인-전용-학습-노트-뷰어--rag-검색-pwa---2026년03월28일">Book Notes App - 개인 전용 학습 노트 뷰어 + RAG 검색 PWA - 2026년03월28일</h1>

<!-- - tag : Python FastAPI LangChain ChromaDB PWA Docker AWS OAuth  -->
<ul>
  <li>tag : <a href=" /tags/tag_Python.html" class="btn btn-default navbar-btn cursorNorm" role="button">Python</a>
|
<a href=" /tags/tag_FastAPI.html" class="btn btn-default navbar-btn cursorNorm" role="button">FastAPI</a>
|
<a href=" /tags/tag_LangChain.html" class="btn btn-default navbar-btn cursorNorm" role="button">LangChain</a>
|
<a href=" /tags/tag_ChromaDB.html" class="btn btn-default navbar-btn cursorNorm" role="button">ChromaDB</a>
|
<a href=" /tags/tag_PWA.html" class="btn btn-default navbar-btn cursorNorm" role="button">PWA</a>
|
<a href=" /tags/tag_Docker.html" class="btn btn-default navbar-btn cursorNorm" role="button">Docker</a>
|
<a href=" /tags/tag_AWS.html" class="btn btn-default navbar-btn cursorNorm" role="button">AWS</a>
|
<a href=" /tags/tag_OAuth.html" class="btn btn-default navbar-btn cursorNorm" role="button">OAuth</a>
|</li>
</ul>

<hr />

<h2 id="프로젝트-소개">프로젝트 소개</h2>

<p>GitHub private repo에 Obsidian으로 정리한 학습 노트를 핸드폰에서 편하게 열람하고, 자연어로 검색할 수 있는 개인 전용 PWA 앱이다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자(핸드폰) → nginx(80/443) → FastAPI(8000) → ChromaDB / GitHub
</code></pre></div></div>

<h3 id="왜-만들었는가">왜 만들었는가</h3>

<ul>
  <li>Obsidian은 PC에서는 편하지만 핸드폰에서 private repo 접근이 불편</li>
  <li>수백 개의 마크다운 파일에서 원하는 내용을 빠르게 찾고 싶었음</li>
  <li>LangChain RAG를 실제 서비스에 적용하는 경험을 쌓고 싶었음</li>
</ul>

<h3 id="기술-스택-선택-이유">기술 스택 선택 이유</h3>

<table>
  <thead>
    <tr>
      <th>기술</th>
      <th>선택 이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>PWA</td>
      <td>스토어 배포 불필요, 서버 배포만으로 업데이트 즉시 반영, 새 언어 학습 불필요</td>
    </tr>
    <tr>
      <td>FastAPI</td>
      <td>비동기 지원, 타입 힌트 기반 자동 검증, Depends로 인증 로직 재사용</td>
    </tr>
    <tr>
      <td>Google OAuth</td>
      <td>비밀번호 직접 관리 불필요, 이메일 화이트리스트로 1인 접근 제어</td>
    </tr>
    <tr>
      <td>ChromaDB</td>
      <td>별도 서버 없이 Python 라이브러리로 벡터 DB 사용, 소규모 문서에 적합</td>
    </tr>
    <tr>
      <td>text-embedding-3-small</td>
      <td>한국어 성능 우수, $0.02/1M 토큰으로 거의 무료</td>
    </tr>
    <tr>
      <td>AWS EC2 + Route 53</td>
      <td>서버/도메인/DNS를 AWS 한 곳에서 통합 관리</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="아키텍처">아키텍처</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>EC2 (Ubuntu, t3.micro, 서울 리전)
│
├── nginx (:80 → :443 리디렉트)
│         (:443 → localhost:8000 프록시)
│         (SSL: Let's Encrypt)
│
└── Docker
    └── FastAPI (:8000)
        ├── Auth (Google OAuth + JWT)
        ├── Docs (마크다운 뷰어)
        ├── Search (ChromaDB + LangChain RAG)
        └── Webhook (GitHub push → 자동 동기화)
</code></pre></div></div>

<h3 id="두-개-repository의-관계">두 개 Repository의 관계</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>book-notes (private)              book-notes-app (private)
├── langchain/                    ├── backend/
│   ├── chapter1.md               │   ├── main.py
│   └── chapter2.md               │   ├── auth/
├── sql/                          │   ├── docs/
│   └── camp.md                   │   └── search/
└── ...                           ├── frontend/
                                  ├── nginx/
    학습 노트 원본                  ├── Dockerfile
    (push → Webhook →              └── docker-compose.yml
     앱에 자동 반영)
                                      앱 코드
                                      (push → EC2 수동 배포)
</code></pre></div></div>

<hr />

<h2 id="phase-1-인증-시스템">Phase 1: 인증 시스템</h2>

<h3 id="google-oauth-20-흐름">Google OAuth 2.0 흐름</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. "Google로 로그인" 클릭
2. → accounts.google.com으로 리디렉트
3. → Google 로그인 + 권한 동의
4. → /auth/callback으로 auth code 전달
5. → auth code를 access token으로 교환
6. → 이메일 추출 → 화이트리스트 확인
7. → JWT 발급 (httpOnly cookie)
</code></pre></div></div>

<h3 id="1인-앱-접근-제어">1인 앱 접근 제어</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .env
</span><span class="n">ALLOWED_EMAIL</span><span class="o">=</span><span class="n">myemail</span><span class="o">@</span><span class="n">gmail</span><span class="p">.</span><span class="n">com</span>

<span class="c1"># 콜백에서 이메일 체크
</span><span class="k">if</span> <span class="n">user_email</span> <span class="o">!=</span> <span class="n">os</span><span class="p">.</span><span class="n">getenv</span><span class="p">(</span><span class="s">"ALLOWED_EMAIL"</span><span class="p">):</span>
    <span class="k">raise</span> <span class="n">HTTPException</span><span class="p">(</span><span class="mi">403</span><span class="p">,</span> <span class="s">"Access denied"</span><span class="p">)</span>
</code></pre></div></div>

<p>Google OAuth 동의 화면을 “테스트 모드”로 설정하면 등록한 테스트 사용자만 로그인 가능하다.</p>

<h3 id="jwt-httponly-cookie">JWT httpOnly Cookie</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">response</span><span class="p">.</span><span class="n">set_cookie</span><span class="p">(</span>
    <span class="n">key</span><span class="o">=</span><span class="s">"access_token"</span><span class="p">,</span>
    <span class="n">value</span><span class="o">=</span><span class="n">token</span><span class="p">,</span>
    <span class="n">httponly</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>   <span class="c1"># JS에서 접근 불가 (XSS 방지)
</span>    <span class="n">secure</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>     <span class="c1"># HTTPS에서만 전송
</span>    <span class="n">samesite</span><span class="o">=</span><span class="s">"lax"</span>   <span class="c1"># CSRF 방지
</span><span class="p">)</span>
</code></pre></div></div>

<h3 id="fastapi-의존성-주입으로-인증-체크">FastAPI 의존성 주입으로 인증 체크</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">fastapi</span> <span class="kn">import</span> <span class="n">Depends</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">get_current_user</span><span class="p">(</span><span class="n">request</span><span class="p">:</span> <span class="n">Request</span><span class="p">):</span>
    <span class="n">token</span> <span class="o">=</span> <span class="n">request</span><span class="p">.</span><span class="n">cookies</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"access_token"</span><span class="p">)</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="n">token</span><span class="p">:</span>
        <span class="k">raise</span> <span class="n">HTTPException</span><span class="p">(</span><span class="mi">401</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">verify_token</span><span class="p">(</span><span class="n">token</span><span class="p">)</span>

<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"/api/docs/tree"</span><span class="p">)</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">get_tree</span><span class="p">(</span><span class="n">user</span><span class="o">=</span><span class="n">Depends</span><span class="p">(</span><span class="n">get_current_user</span><span class="p">)):</span>
    <span class="k">return</span> <span class="p">{</span><span class="s">"tree"</span><span class="p">:</span> <span class="n">build_tree</span><span class="p">()}</span>
</code></pre></div></div>

<p>모든 API 엔드포인트에 <code class="language-plaintext highlighter-rouge">Depends(get_current_user)</code>를 추가하면 인증이 필수가 된다.</p>

<h3 id="트러블슈팅-mismatchingstateerror">트러블슈팅: MismatchingStateError</h3>

<p>OAuth 콜백에서 세션 쿠키가 유실되어 발생한 문제.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 원인: SessionMiddleware의 https_only 설정
# localhost(HTTP)에서 쿠키가 전송되지 않음
</span>
<span class="c1"># 해결: 개발 환경에서 https_only=False
</span><span class="n">app</span><span class="p">.</span><span class="n">add_middleware</span><span class="p">(</span>
    <span class="n">SessionMiddleware</span><span class="p">,</span>
    <span class="n">secret_key</span><span class="o">=</span><span class="n">settings</span><span class="p">.</span><span class="n">JWT_SECRET_KEY</span><span class="p">,</span>
    <span class="n">same_site</span><span class="o">=</span><span class="s">"lax"</span><span class="p">,</span>
    <span class="n">https_only</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>  <span class="c1"># 로컬 개발 시
</span><span class="p">)</span>
</code></pre></div></div>

<hr />

<h2 id="phase-2-문서-동기화--뷰어">Phase 2: 문서 동기화 + 뷰어</h2>

<h3 id="github-private-repo-동기화">GitHub Private Repo 동기화</h3>

<p>GitPython으로 PAT(Personal Access Token)을 사용하여 private repo를 clone/pull한다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">git</span>

<span class="k">def</span> <span class="nf">clone_or_pull</span><span class="p">():</span>
    <span class="n">repo_url</span> <span class="o">=</span> <span class="sa">f</span><span class="s">"https://</span><span class="si">{</span><span class="n">PAT</span><span class="si">}</span><span class="s">@github.com/</span><span class="si">{</span><span class="n">GITHUB_REPO</span><span class="si">}</span><span class="s">.git"</span>
    <span class="k">if</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="n">exists</span><span class="p">(</span><span class="n">DOCS_DIR</span><span class="p">):</span>
        <span class="n">repo</span> <span class="o">=</span> <span class="n">git</span><span class="p">.</span><span class="n">Repo</span><span class="p">(</span><span class="n">DOCS_DIR</span><span class="p">)</span>
        <span class="n">repo</span><span class="p">.</span><span class="n">remotes</span><span class="p">.</span><span class="n">origin</span><span class="p">.</span><span class="n">pull</span><span class="p">()</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">git</span><span class="p">.</span><span class="n">Repo</span><span class="p">.</span><span class="n">clone_from</span><span class="p">(</span><span class="n">repo_url</span><span class="p">,</span> <span class="n">DOCS_DIR</span><span class="p">,</span> <span class="n">depth</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">depth=1</code>: 최신 커밋만 가져옴 (히스토리 불필요)</li>
  <li>PAT은 Fine-grained token으로 해당 repo의 Contents Read-only 권한만 부여</li>
</ul>

<h3 id="github-webhook-push-즉시-반영">GitHub Webhook (push 즉시 반영)</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">hmac</span><span class="p">,</span> <span class="n">hashlib</span>
<span class="kn">from</span> <span class="nn">fastapi</span> <span class="kn">import</span> <span class="n">BackgroundTasks</span>

<span class="k">def</span> <span class="nf">verify_signature</span><span class="p">(</span><span class="n">payload</span><span class="p">:</span> <span class="nb">bytes</span><span class="p">,</span> <span class="n">signature</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">secret</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">bool</span><span class="p">:</span>
    <span class="n">expected</span> <span class="o">=</span> <span class="s">"sha256="</span> <span class="o">+</span> <span class="n">hmac</span><span class="p">.</span><span class="n">new</span><span class="p">(</span>
        <span class="n">secret</span><span class="p">.</span><span class="n">encode</span><span class="p">(),</span> <span class="n">payload</span><span class="p">,</span> <span class="n">hashlib</span><span class="p">.</span><span class="n">sha256</span>
    <span class="p">).</span><span class="n">hexdigest</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">hmac</span><span class="p">.</span><span class="n">compare_digest</span><span class="p">(</span><span class="n">expected</span><span class="p">,</span> <span class="n">signature</span><span class="p">)</span>

<span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">post</span><span class="p">(</span><span class="s">"/webhook/github"</span><span class="p">)</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">webhook</span><span class="p">(</span><span class="n">request</span><span class="p">:</span> <span class="n">Request</span><span class="p">,</span> <span class="n">background_tasks</span><span class="p">:</span> <span class="n">BackgroundTasks</span><span class="p">):</span>
    <span class="n">payload</span> <span class="o">=</span> <span class="k">await</span> <span class="n">request</span><span class="p">.</span><span class="n">body</span><span class="p">()</span>
    <span class="n">signature</span> <span class="o">=</span> <span class="n">request</span><span class="p">.</span><span class="n">headers</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"X-Hub-Signature-256"</span><span class="p">,</span> <span class="s">""</span><span class="p">)</span>

    <span class="k">if</span> <span class="ow">not</span> <span class="n">verify_signature</span><span class="p">(</span><span class="n">payload</span><span class="p">,</span> <span class="n">signature</span><span class="p">,</span> <span class="n">WEBHOOK_SECRET</span><span class="p">):</span>
        <span class="k">raise</span> <span class="n">HTTPException</span><span class="p">(</span><span class="mi">403</span><span class="p">)</span>

    <span class="n">background_tasks</span><span class="p">.</span><span class="n">add_task</span><span class="p">(</span><span class="n">sync_and_reindex</span><span class="p">)</span>
    <span class="k">return</span> <span class="p">{</span><span class="s">"status"</span><span class="p">:</span> <span class="s">"accepted"</span><span class="p">}</span>  <span class="c1"># 즉시 200 응답
</span></code></pre></div></div>

<ul>
  <li>HMAC-SHA256 서명으로 GitHub이 보낸 요청인지 검증</li>
  <li>BackgroundTasks로 즉시 200 응답 후 백그라운드에서 동기화 (GitHub 10초 타임아웃 대응)</li>
</ul>

<h3 id="마크다운-파서--문서-트리">마크다운 파서 + 문서 트리</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_doc_tree</span><span class="p">(</span><span class="n">base_dir</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="s">"""디렉토리 구조를 JSON 트리로 변환. .md 파일만 대상."""</span>
    <span class="n">tree</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">path</span> <span class="ow">in</span> <span class="n">Path</span><span class="p">(</span><span class="n">base_dir</span><span class="p">).</span><span class="n">rglob</span><span class="p">(</span><span class="s">"*.md"</span><span class="p">):</span>
        <span class="n">relative</span> <span class="o">=</span> <span class="n">path</span><span class="p">.</span><span class="n">relative_to</span><span class="p">(</span><span class="n">base_dir</span><span class="p">)</span>
        <span class="c1"># path traversal 방어: .. 포함된 경로 거부
</span>        <span class="k">if</span> <span class="s">".."</span> <span class="ow">in</span> <span class="nb">str</span><span class="p">(</span><span class="n">relative</span><span class="p">):</span>
            <span class="k">continue</span>
        <span class="c1"># 트리 구조 생성
</span>        <span class="p">...</span>
    <span class="k">return</span> <span class="n">tree</span>
</code></pre></div></div>

<h3 id="프론트엔드-구성">프론트엔드 구성</h3>

<ul>
  <li>SPA 라우팅: hash 기반 (<code class="language-plaintext highlighter-rouge">#/docs</code>, <code class="language-plaintext highlighter-rouge">#/docs/path</code>, <code class="language-plaintext highlighter-rouge">#/search</code>)</li>
  <li>마크다운 렌더링: marked.js (CDN)</li>
  <li>코드 하이라이팅: highlight.js (CDN)</li>
  <li>모바일 퍼스트: 768px 이하에서 사이드바를 슬라이드 메뉴로 변환</li>
</ul>

<hr />

<h2 id="phase-3-rag-검색">Phase 3: RAG 검색</h2>

<h3 id="rag-흐름">RAG 흐름</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자 질문: "LangChain에서 프롬프트 템플릿 사용법은?"
    ↓
1. 질문을 text-embedding-3-small로 임베딩
    ↓
2. ChromaDB에서 유사한 문서 청크 top-5 검색 (코사인 유사도)
    ↓
3. 검색된 문서 + 질문을 합쳐서 gpt-4.1-mini 프롬프트 구성
    ↓
4. LLM이 문서 기반으로 답변 생성
    ↓
5. 답변 + 출처(파일 경로) 반환
</code></pre></div></div>

<h3 id="청킹-전략">청킹 전략</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">langchain.text_splitter</span> <span class="kn">import</span> <span class="n">RecursiveCharacterTextSplitter</span>

<span class="n">splitter</span> <span class="o">=</span> <span class="n">RecursiveCharacterTextSplitter</span><span class="p">(</span>
    <span class="n">chunk_size</span><span class="o">=</span><span class="mi">1000</span><span class="p">,</span>
    <span class="n">chunk_overlap</span><span class="o">=</span><span class="mi">200</span><span class="p">,</span>
    <span class="n">separators</span><span class="o">=</span><span class="p">[</span><span class="s">"## "</span><span class="p">,</span> <span class="s">"### "</span><span class="p">,</span> <span class="s">"</span><span class="se">\n\n</span><span class="s">"</span><span class="p">,</span> <span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">]</span>
<span class="p">)</span>
</code></pre></div></div>

<ul>
  <li>마크다운 헤더(<code class="language-plaintext highlighter-rouge">##</code>, <code class="language-plaintext highlighter-rouge">###</code>) 기준으로 의미 단위 분할</li>
  <li>chunk_overlap=200: 청크 경계에서 문맥 유실 방지</li>
  <li>메타데이터에 파일 경로 포함 → 출처 추적</li>
</ul>

<h3 id="환각-방지-프롬프트">환각 방지 프롬프트</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">system_prompt</span> <span class="o">=</span> <span class="s">"""
당신은 학습 노트 검색 도우미입니다.
주어진 문서 내용만을 기반으로 답변하세요.
문서에 없는 내용은 "해당 내용을 찾을 수 없습니다"라고 답하세요.
"""</span>
</code></pre></div></div>

<h3 id="chromadb-벡터-저장소">ChromaDB 벡터 저장소</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">chromadb</span>

<span class="n">client</span> <span class="o">=</span> <span class="n">chromadb</span><span class="p">.</span><span class="n">PersistentClient</span><span class="p">(</span><span class="n">path</span><span class="o">=</span><span class="s">"./data/chroma"</span><span class="p">)</span>
<span class="n">collection</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">get_or_create_collection</span><span class="p">(</span><span class="s">"book_notes"</span><span class="p">)</span>

<span class="c1"># 문서 저장
</span><span class="n">collection</span><span class="p">.</span><span class="n">add</span><span class="p">(</span>
    <span class="n">documents</span><span class="o">=</span><span class="n">chunks</span><span class="p">,</span>
    <span class="n">embeddings</span><span class="o">=</span><span class="n">vectors</span><span class="p">,</span>
    <span class="n">metadatas</span><span class="o">=</span><span class="p">[{</span><span class="s">"source"</span><span class="p">:</span> <span class="s">"langchain/chapter3.md"</span><span class="p">}],</span>
    <span class="n">ids</span><span class="o">=</span><span class="p">[</span><span class="s">"chunk_001"</span><span class="p">]</span>
<span class="p">)</span>

<span class="c1"># 유사도 검색
</span><span class="n">results</span> <span class="o">=</span> <span class="n">collection</span><span class="p">.</span><span class="n">query</span><span class="p">(</span><span class="n">query_embeddings</span><span class="o">=</span><span class="p">[</span><span class="n">query_vector</span><span class="p">],</span> <span class="n">n_results</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>
</code></pre></div></div>

<ul>
  <li>PersistentClient: 디스크에 저장 → Docker 볼륨으로 영속화</li>
  <li>별도 서버 없이 Python 라이브러리로 사용</li>
</ul>

<hr />

<h2 id="phase-4-pwa--docker--배포">Phase 4: PWA + Docker + 배포</h2>

<h3 id="pwa-설정">PWA 설정</h3>

<p>manifest.json + Service Worker를 추가하면 브라우저가 “설치 가능한 앱”으로 인식한다.</p>

<ul>
  <li>Android: Chrome 메뉴 → “홈 화면에 추가”</li>
  <li>iOS: Safari 공유 버튼 → “홈 화면에 추가”</li>
</ul>

<p>Service Worker 캐싱 전략:</p>

<table>
  <thead>
    <tr>
      <th>요청</th>
      <th>전략</th>
      <th>이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>정적 파일 (HTML, CSS, JS)</td>
      <td>Cache First</td>
      <td>오프라인에서도 UI 로드</td>
    </tr>
    <tr>
      <td>API 요청 (/api/*)</td>
      <td>Network First</td>
      <td>최신 데이터 필요</td>
    </tr>
    <tr>
      <td>sw.js 자체</td>
      <td>no-cache</td>
      <td>항상 최신 버전 보장</td>
    </tr>
  </tbody>
</table>

<h3 id="dockerfile-멀티스테이지-빌드">Dockerfile (멀티스테이지 빌드)</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 빌드 스테이지</span>
<span class="k">FROM</span><span class="w"> </span><span class="s">python:3.11-slim</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="s">builder</span>
<span class="k">COPY</span><span class="s"> pyproject.toml uv.lock ./</span>
<span class="k">RUN </span>uv <span class="nb">sync</span> <span class="nt">--frozen</span>

<span class="c"># 런타임 스테이지</span>
<span class="k">FROM</span><span class="s"> python:3.11-slim</span>
<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> apt-get <span class="nb">install</span> <span class="nt">-y</span> git <span class="o">&amp;&amp;</span> <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>
<span class="k">COPY</span><span class="s"> --from=builder /app/.venv .venv</span>
<span class="k">COPY</span><span class="s"> . .</span>
<span class="k">CMD</span><span class="s"> ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]</span>
</code></pre></div></div>

<ul>
  <li>멀티스테이지: 빌드 도구는 builder에만 설치 → 최종 이미지 경량화</li>
  <li>git 설치 필수: GitPython이 git CLI를 내부적으로 호출</li>
</ul>

<h3 id="docker-composeyml">docker-compose.yml</h3>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">services</span><span class="pi">:</span>
  <span class="na">app</span><span class="pi">:</span>
    <span class="na">build</span><span class="pi">:</span> <span class="s">.</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s2">"</span><span class="s">8000:8000"</span>
    <span class="na">env_file</span><span class="pi">:</span> <span class="s">.env</span>
    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">chroma_data:/app/data/chroma</span>
      <span class="pi">-</span> <span class="s">docs_data:/app/data/docs</span>

<span class="na">volumes</span><span class="pi">:</span>
  <span class="na">chroma_data</span><span class="pi">:</span>
  <span class="na">docs_data</span><span class="pi">:</span>
</code></pre></div></div>

<ul>
  <li>volumes: 컨테이너 삭제/재생성해도 ChromaDB 데이터와 동기화 문서 유지</li>
</ul>

<h3 id="nginx-리버스-프록시">nginx 리버스 프록시</h3>

<div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># HTTP → HTTPS 리디렉트</span>
<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span><span class="p">;</span>
    <span class="kn">server_name</span> <span class="s">booknotesapp.com</span><span class="p">;</span>
    <span class="kn">return</span> <span class="mi">301</span> <span class="s">https://</span><span class="nv">$host$request_uri</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1"># HTTPS 서버</span>
<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">443</span> <span class="s">ssl</span> <span class="s">http2</span><span class="p">;</span>
    <span class="kn">server_name</span> <span class="s">booknotesapp.com</span><span class="p">;</span>

    <span class="kn">ssl_certificate</span>     <span class="n">/etc/letsencrypt/live/booknotesapp.com/fullchain.pem</span><span class="p">;</span>
    <span class="kn">ssl_certificate_key</span> <span class="n">/etc/letsencrypt/live/booknotesapp.com/privkey.pem</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/api/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://127.0.0.1:8000</span><span class="p">;</span>
        <span class="kn">proxy_read_timeout</span> <span class="s">120s</span><span class="p">;</span>  <span class="c1"># RAG 검색 대기 시간</span>
    <span class="p">}</span>

    <span class="kn">location</span> <span class="n">/webhook/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://127.0.0.1:8000</span><span class="p">;</span>
        <span class="kn">proxy_read_timeout</span> <span class="s">30s</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>SSL 종료를 nginx에서 처리 → FastAPI는 HTTP만 담당</li>
  <li>RAG 검색은 응답이 느릴 수 있어 120초 타임아웃 설정</li>
</ul>

<h3 id="aws-배포-절차">AWS 배포 절차</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. EC2 인스턴스 생성 (t3.micro, Ubuntu, 서울 리전)</span>
<span class="c"># 2. 보안 그룹: SSH(22), HTTP(80), HTTPS(443) 오픈</span>
<span class="c"># 3. Elastic IP 할당 + 인스턴스 연결</span>
<span class="c"># 4. Route 53 도메인 등록 + A 레코드에 Elastic IP 설정</span>

<span class="c"># EC2 접속</span>
ssh <span class="nt">-i</span> <span class="s2">"키파일.pem"</span> ubuntu@3.35.40.73

<span class="c"># Docker 설치</span>
<span class="nb">sudo </span>apt update <span class="o">&amp;&amp;</span> <span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> docker.io docker-compose-v2
<span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker ubuntu

<span class="c"># 코드 배포</span>
git clone https://github.com/username/book-notes-app.git
<span class="nb">cd </span>book-notes-app
nano .env  <span class="c"># 환경변수 설정</span>

<span class="c"># SSL 인증서 발급</span>
<span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> certbot
<span class="nb">sudo </span>certbot certonly <span class="nt">--standalone</span> <span class="nt">-d</span> booknotesapp.com

<span class="c"># nginx 설치 + 설정</span>
<span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> nginx
<span class="nb">sudo cp </span>nginx/default.conf /etc/nginx/sites-available/default
<span class="nb">sudo </span>nginx <span class="nt">-t</span>
<span class="nb">sudo </span>systemctl restart nginx

<span class="c"># Docker 실행</span>
<span class="nb">sudo </span>docker compose up <span class="nt">-d</span>
</code></pre></div></div>

<h3 id="lets-encrypt-인증서-발급-주의사항">Let’s Encrypt 인증서 발급 주의사항</h3>

<ul>
  <li>발급 시 포트 80이 비어있어야 함 (Docker/nginx 중지 후 발급)</li>
  <li>EC2 보안 그룹에서 80, 443 포트 열림 확인 필수</li>
  <li>인증서 유효기간 90일 (certbot이 자동 갱신 설정)</li>
</ul>

<hr />

<h2 id="트러블슈팅-모음">트러블슈팅 모음</h2>

<h3 id="hatchling-빌드-에러">hatchling 빌드 에러</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ValueError: Unable to determine which files to ship inside the wheel
</code></pre></div></div>

<p>pyproject.toml에 빌드 대상 패키지를 명시하지 않아서 발생.</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[tool.hatch.build.targets.wheel]</span>
<span class="py">packages</span> <span class="p">=</span> <span class="nn">["backend"]</span>
</code></pre></div></div>

<h3 id="docker에서-git-없음">Docker에서 git 없음</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ImportError: Failed to initialize: Bad git executable.
</code></pre></div></div>

<p>Dockerfile runtime 스테이지에 git 설치 누락. <code class="language-plaintext highlighter-rouge">apt-get install -y git</code> 추가.</p>

<h3 id="certbot-타임아웃">certbot 타임아웃</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Detail: Fetching http://booknotesapp.com/...: Timeout during connect
</code></pre></div></div>

<p>EC2 보안 그룹에 HTTP(80) 규칙이 없거나, Docker가 포트 80을 점유 중.
보안 그룹 확인 + <code class="language-plaintext highlighter-rouge">docker compose down</code> 후 재시도.</p>

<h3 id="oauth-콜백에서-localhost로-리디렉트">OAuth 콜백에서 localhost로 리디렉트</h3>

<p><code class="language-plaintext highlighter-rouge">.env</code>의 <code class="language-plaintext highlighter-rouge">APP_URL</code>이 <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>으로 되어있으면 발생.
<code class="language-plaintext highlighter-rouge">APP_URL=https://booknotesapp.com</code>으로 변경 + Google Cloud Console에 프로덕션 리디렉션 URI 추가.</p>

<hr />

<h2 id="월-비용">월 비용</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>비용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>EC2 t3.micro</td>
      <td>~$8.5 (Free Tier 1년 무료)</td>
    </tr>
    <tr>
      <td>EBS 8GB</td>
      <td>~$0.8</td>
    </tr>
    <tr>
      <td>Route 53 도메인</td>
      <td>~$13/년</td>
    </tr>
    <tr>
      <td>Route 53 호스팅</td>
      <td>~$0.5/월</td>
    </tr>
    <tr>
      <td>OpenAI API</td>
      <td>~$0.5</td>
    </tr>
    <tr>
      <td>Let’s Encrypt / OAuth / ChromaDB</td>
      <td>무료</td>
    </tr>
    <tr>
      <td><strong>합계</strong></td>
      <td><strong>~$10~11/월</strong></td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="향후-개선-사항">향후 개선 사항</h2>

<ul>
  <li>검색 결과 스트리밍 (SSE)</li>
  <li>다크 모드 지원</li>
  <li>문서 즐겨찾기 / 최근 본 문서</li>
  <li>Airflow DAG로 자동 동기화 + 인덱싱 파이프라인 (LLMOps 연계)</li>
  <li>LangSmith 트레이싱 연동</li>
  <li>GitHub Actions CI/CD로 앱 코드 자동 배포</li>
</ul>]]></content><author><name>INSU</name></author><category term="Python" /><category term="FastAPI" /><category term="LangChain" /><category term="ChromaDB" /><category term="PWA" /><category term="Docker" /><category term="AWS" /><category term="OAuth" /><summary type="html"><![CDATA[Book Notes App - 개인 전용 학습 노트 뷰어 + RAG 검색 PWA - 2026년03월28일 tag : Python | FastAPI | LangChain | ChromaDB | PWA | Docker | AWS | OAuth |]]></summary></entry><entry><title type="html">Python 환경 및 패키지 관리</title><link href="http://localhost:4000/2026/03/11/Python_Env_and_Package.html" rel="alternate" type="text/html" title="Python 환경 및 패키지 관리" /><published>2026-03-11T00:00:00+09:00</published><updated>2026-03-11T00:00:00+09:00</updated><id>http://localhost:4000/2026/03/11/Python_Env_and_Package</id><content type="html" xml:base="http://localhost:4000/2026/03/11/Python_Env_and_Package.html"><![CDATA[<h1 id="python-환경-및-패키지-관리---2026년03월11일">Python 환경 및 패키지 관리 - 2026년03월11일</h1>

<!-- - tag : Python Python_Setup  -->
<ul>
  <li>tag : <a href=" /tags/tag_Python.html" class="btn btn-default navbar-btn cursorNorm" role="button">Python</a>
|
<a href=" /tags/tag_Python_Setup.html" class="btn btn-default navbar-btn cursorNorm" role="button">Python_Setup</a>
|</li>
</ul>

<hr />

<h2 id="가상환경-설정">가상환경 설정</h2>

<h3 id="windows---anaconda">Windows - Anaconda</h3>

<p><strong>명령어 형식</strong></p>

<pre><code class="language-cmd">conda create -n {가상환경명} python={파이썬버전}
conda activate {가상환경명}
conda deactivate
</code></pre>

<p><strong>예시</strong></p>

<pre><code class="language-cmd">conda create -n venv python=3.11
conda activate venv
conda deactivate
</code></pre>

<h3 id="windows---uv">Windows - uv</h3>

<pre><code class="language-cmd">pip install uv
uv venv --python 3.11
.venv\Scripts\activate
</code></pre>

<h2 id="라이브러리-관리">라이브러리 관리</h2>

<h3 id="방법-1-pip-freeze-기반">방법 1: pip freeze 기반</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip freeze <span class="o">&gt;</span> requirements.txt
pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
</code></pre></div></div>

<h3 id="방법-2-pipreqs-기반">방법 2: pipreqs 기반</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>pipreqs
pipreqs ./ <span class="nt">--force</span>

<span class="c"># 폴더 내에 가상환경이 있는 경우</span>
pipreqs <span class="nb">.</span> <span class="nt">--force</span> <span class="nt">--ignore</span> <span class="s2">"{가상환경}/Lib"</span>
</code></pre></div></div>

<h3 id="방법-3-특정-패키지-점검">방법 3: 특정 패키지 점검</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip freeze | <span class="nb">grep </span>cohere
</code></pre></div></div>]]></content><author><name>INSU</name></author><category term="Python" /><category term="Python_Setup" /><summary type="html"><![CDATA[Python 환경 및 패키지 관리 - 2026년03월11일 tag : Python | Python_Setup |]]></summary></entry><entry><title type="html">Python Typst 이미지 변환</title><link href="http://localhost:4000/2026/03/11/Python_Typst_Image.html" rel="alternate" type="text/html" title="Python Typst 이미지 변환" /><published>2026-03-11T00:00:00+09:00</published><updated>2026-03-11T00:00:00+09:00</updated><id>http://localhost:4000/2026/03/11/Python_Typst_Image</id><content type="html" xml:base="http://localhost:4000/2026/03/11/Python_Typst_Image.html"><![CDATA[<h1 id="python-typst-이미지-변환---2026년03월11일">Python Typst 이미지 변환 - 2026년03월11일</h1>

<!-- - tag : Python Typst  -->
<ul>
  <li>tag : <a href=" /tags/tag_Python.html" class="btn btn-default navbar-btn cursorNorm" role="button">Python</a>
|
<a href=" /tags/tag_Typst.html" class="btn btn-default navbar-btn cursorNorm" role="button">Typst</a>
|</li>
</ul>

<hr />

<h2 id="이미지-변환-webp---png">이미지 변환 (webp -&gt; png)</h2>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">PIL</span> <span class="kn">import</span> <span class="n">Image</span>

<span class="n">img</span> <span class="o">=</span> <span class="n">Image</span><span class="p">.</span><span class="nb">open</span><span class="p">(</span><span class="s">"image.webp"</span><span class="p">)</span>
<span class="n">img</span><span class="p">.</span><span class="n">save</span><span class="p">(</span><span class="s">"image.png"</span><span class="p">,</span> <span class="s">"PNG"</span><span class="p">)</span>
</code></pre></div></div>]]></content><author><name>INSU</name></author><category term="Python" /><category term="Typst" /><summary type="html"><![CDATA[Python Typst 이미지 변환 - 2026년03월11일 tag : Python | Typst |]]></summary></entry><entry><title type="html">Python RAG Reranker 오류 해결</title><link href="http://localhost:4000/2026/03/11/Python_RAG_Troubleshooting.html" rel="alternate" type="text/html" title="Python RAG Reranker 오류 해결" /><published>2026-03-11T00:00:00+09:00</published><updated>2026-03-11T00:00:00+09:00</updated><id>http://localhost:4000/2026/03/11/Python_RAG_Troubleshooting</id><content type="html" xml:base="http://localhost:4000/2026/03/11/Python_RAG_Troubleshooting.html"><![CDATA[<h1 id="python-rag-reranker-오류-해결---2026년03월11일">Python RAG Reranker 오류 해결 - 2026년03월11일</h1>

<!-- - tag : Python Troubleshooting RAG  -->
<ul>
  <li>tag : <a href=" /tags/tag_Python.html" class="btn btn-default navbar-btn cursorNorm" role="button">Python</a>
|
<a href=" /tags/tag_Troubleshooting.html" class="btn btn-default navbar-btn cursorNorm" role="button">Troubleshooting</a>
|
<a href=" /tags/tag_RAG.html" class="btn btn-default navbar-btn cursorNorm" role="button">RAG</a>
|</li>
</ul>

<hr />

<h2 id="rag---reranker">RAG - Reranker</h2>

<h3 id="재현-코드">재현 코드</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">langchain_community.cross_encoders</span> <span class="kn">import</span> <span class="n">HuggingFaceCrossEncoder</span>

<span class="n">hf_reranker</span> <span class="o">=</span> <span class="n">HuggingFaceCrossEncoder</span><span class="p">(</span>
    <span class="n">model_name</span><span class="o">=</span><span class="s">"BAAI/bge-reranker-v2-m3"</span>
<span class="p">)</span>
</code></pre></div></div>

<h3 id="증상-오류-메시지">증상 (오류 메시지)</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Error rendering output item using 'jupyter-ipywidget-renderer'
Cannot read properties of undefined (reading 'ipywidgetsKernel')
</code></pre></div></div>

<h3 id="원인">원인</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ipywidgets</code> 렌더러/확장 모듈 미설치 또는 버전 불일치</li>
</ul>

<h3 id="해결-방법">해결 방법</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>ipywidgets <span class="nt">--upgrade</span>
jupyter nbextension <span class="nb">enable</span> <span class="nt">--py</span> widgetsnbextension <span class="nt">--sys-prefix</span>
jupyter nbextension <span class="nb">install</span> <span class="nt">--py</span> widgetsnbextension <span class="nt">--sys-prefix</span>
</code></pre></div></div>]]></content><author><name>INSU</name></author><category term="Python" /><category term="Troubleshooting" /><category term="RAG" /><summary type="html"><![CDATA[Python RAG Reranker 오류 해결 - 2026년03월11일 tag : Python | Troubleshooting | RAG |]]></summary></entry><entry><title type="html">배드민턴 통계 챗봇 개발기</title><link href="http://localhost:4000/2026/03/05/badminton_stats_chatbot.html" rel="alternate" type="text/html" title="배드민턴 통계 챗봇 개발기" /><published>2026-03-05T00:00:00+09:00</published><updated>2026-03-05T00:00:00+09:00</updated><id>http://localhost:4000/2026/03/05/badminton_stats_chatbot</id><content type="html" xml:base="http://localhost:4000/2026/03/05/badminton_stats_chatbot.html"><![CDATA[<h1 id="배드민턴-통계-챗봇-개발기---2026년03월05일">배드민턴 통계 챗봇 개발기 - 2026년03월05일</h1>

<!-- - tag : Project RAG  -->
<ul>
  <li>tag : <a href=" /tags/tag_Project.html" class="btn btn-default navbar-btn cursorNorm" role="button">Project</a>
|
<a href=" /tags/tag_RAG.html" class="btn btn-default navbar-btn cursorNorm" role="button">RAG</a>
|</li>
</ul>

<hr />

<h2 id="프로젝트-개요">프로젝트 개요</h2>

<p>배드민턴 동호회 경기 기록을 자연어로 조회할 수 있는 AI 챗봇을 개발했습니다.</p>

<ul>
  <li><strong>기간</strong>: 2026.02 ~ 2026.03 (개인 프로젝트)</li>
  <li><strong>배포</strong>: <a href="https://bm-stats-chatbot.streamlit.app">https://bm-stats-chatbot.streamlit.app</a></li>
  <li><strong>GitHub</strong>: <a href="https://github.com/insu97/badminton-stats-chatbot">https://github.com/insu97/badminton-stats-chatbot</a></li>
</ul>

<hr />

<h2 id="배경-및-문제-정의">배경 및 문제 정의</h2>

<p>배드민턴 동호회를 운영하면서 경기 기록을 Google Sheets로 관리하고 있었습니다.<br />
매번 승률을 확인하려면 스프레드시트를 직접 열고 필터링해야 했고,<br />
경기 후기 같은 정성적인 데이터는 별도 텍스트 파일로 흩어져 있었습니다.</p>

<p><strong>핵심 문제:</strong></p>
<ul>
  <li>통계 조회가 불편 (매번 수작업)</li>
  <li>정량 데이터(승률, 득실점)와 정성 데이터(후기)가 분리되어 있음</li>
  <li>자연어로 “내 승률이 어때?” 같은 질문에 바로 답할 수 없음</li>
</ul>

<hr />

<h2 id="해결-아키텍처">해결 아키텍처</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User Prompt
    │
    ▼
Hybrid Router (LLM)
    ├── 통계 질문 → Text-to-SQL → SQLite → 결과 반환
    └── 후기 질문 → RAG → FAISS Vector DB → 결과 반환
    │
    ▼
LLM Response (GPT-4o-mini)
</code></pre></div></div>

<p>질문 의도를 LLM이 분석해서 <strong>정량 분석(SQL)</strong> 과 <strong>정성 분석(RAG)</strong> 중 적합한 방식으로 자동 라우팅합니다.</p>

<hr />

<h2 id="기술-스택">기술 스택</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>기술</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Language</td>
      <td>Python 3.13</td>
    </tr>
    <tr>
      <td>Framework</td>
      <td>LangChain, Streamlit</td>
    </tr>
    <tr>
      <td>LLM</td>
      <td>OpenAI GPT-4.1-mini</td>
    </tr>
    <tr>
      <td>DB (정형)</td>
      <td>SQLite</td>
    </tr>
    <tr>
      <td>DB (비정형)</td>
      <td>FAISS (Vector DB)</td>
    </tr>
    <tr>
      <td>데이터 소스</td>
      <td>Google Sheets API</td>
    </tr>
    <tr>
      <td>배포</td>
      <td>Streamlit Cloud</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="주요-구현">주요 구현</h2>

<h3 id="1-db-자동-생성-db_loaderpy">1. DB 자동 생성 (db_loader.py)</h3>

<p>Google Sheets에서 경기 기록을 불러와 SQLite DB를 자동 생성합니다.<br />
통계는 직접 입력받지 않고, 경기 기록에서 <strong>자동 계산</strong>합니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">calc_player_stats</span><span class="p">(</span><span class="n">matches_df</span><span class="p">):</span>
    <span class="s">"""경기 기록에서 개인 통계 자동 계산"""</span>
    <span class="n">stats</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">row</span> <span class="ow">in</span> <span class="n">matches_df</span><span class="p">.</span><span class="n">iterrows</span><span class="p">():</span>
        <span class="c1"># 승자/패자 분류 후 승률, 득실점 계산
</span>        <span class="p">...</span>
    <span class="k">return</span> <span class="n">pd</span><span class="p">.</span><span class="n">DataFrame</span><span class="p">(</span><span class="n">stats</span><span class="p">).</span><span class="n">T</span>
</code></pre></div></div>

<p>시즌 컬럼을 자동 감지해서 전체 통계와 시즌별 통계를 모두 생성합니다.</p>

<h3 id="2-rag-파이프라인-rag_loaderpy">2. RAG 파이프라인 (rag_loader.py)</h3>

<p>경기 후기 텍스트 파일을 벡터화해서 FAISS에 저장합니다.<br />
파일명에서 날짜를 추출해 메타데이터로 추가, 최신 경기 후기 검색 정확도를 높였습니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">load_reviews</span><span class="p">():</span>
    <span class="n">docs</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="nb">file</span> <span class="ow">in</span> <span class="n">glob</span><span class="p">(</span><span class="s">"data/reviews/[!sample]*.txt"</span><span class="p">):</span>
        <span class="c1"># 파일명에서 날짜 추출
</span>        <span class="n">date</span> <span class="o">=</span> <span class="n">extract_date_from_filename</span><span class="p">(</span><span class="nb">file</span><span class="p">)</span>
        <span class="n">content</span> <span class="o">=</span> <span class="nb">open</span><span class="p">(</span><span class="nb">file</span><span class="p">).</span><span class="n">read</span><span class="p">()</span>
        <span class="n">docs</span><span class="p">.</span><span class="n">append</span><span class="p">(</span><span class="n">Document</span><span class="p">(</span>
            <span class="n">page_content</span><span class="o">=</span><span class="sa">f</span><span class="s">"[날짜: </span><span class="si">{</span><span class="n">date</span><span class="si">}</span><span class="s">]</span><span class="se">\n</span><span class="si">{</span><span class="n">content</span><span class="si">}</span><span class="s">"</span><span class="p">,</span>
            <span class="n">metadata</span><span class="o">=</span><span class="p">{</span><span class="s">"date"</span><span class="p">:</span> <span class="n">date</span><span class="p">}</span>
        <span class="p">))</span>
    <span class="k">return</span> <span class="n">docs</span>
</code></pre></div></div>

<h3 id="3-하이브리드-라우터-chainpy">3. 하이브리드 라우터 (chain.py)</h3>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">route_question</span><span class="p">(</span><span class="n">question</span><span class="p">):</span>
    <span class="s">"""질문 의도에 따라 SQL 또는 RAG 선택"""</span>
    <span class="n">router_prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="s">"""
    다음 질문이 통계/수치 관련이면 'sql',
    경기 후기/느낌 관련이면 'rag'로만 답하세요.
    질문: </span><span class="si">{</span><span class="n">question</span><span class="si">}</span><span class="s">
    """</span>
    <span class="n">intent</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="n">invoke</span><span class="p">(</span><span class="n">router_prompt</span><span class="p">).</span><span class="n">content</span><span class="p">.</span><span class="n">strip</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">intent</span>
</code></pre></div></div>

<hr />

<h2 id="트러블슈팅">트러블슈팅</h2>

<h3 id="하이픈-포함-컬럼명-sql-오류">하이픈 포함 컬럼명 SQL 오류</h3>

<p><code class="language-plaintext highlighter-rouge">팀A-1</code>, <code class="language-plaintext highlighter-rouge">팀B-1</code> 같은 컬럼명을 SQLite가 빼기 연산자로 인식하는 문제가 있었습니다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># 오류
SELECT 팀A-1 FROM matches  →  팀A 마이너스 1로 해석

# 해결: 프롬프트에 규칙 추가
"하이픈 포함 컬럼명은 큰따옴표로 감싸세요"
SELECT "팀A-1" FROM matches  →  정상 동작
</code></pre></div></div>

<h3 id="rag-날짜-컨텍스트-문제">RAG 날짜 컨텍스트 문제</h3>

<p>“가장 마지막 경기 후기” 질문 시 최신 파일이 아닌 오래된 파일이 검색되는 문제가 있었습니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 해결: 검색 쿼리에 날짜 명시적 추가
</span><span class="n">query</span> <span class="o">=</span> <span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">latest_date</span><span class="si">}</span><span class="s"> 날짜 경기 </span><span class="si">{</span><span class="n">question</span><span class="si">}</span><span class="s">"</span>
</code></pre></div></div>

<h3 id="괄호-불균형-sql-생성">괄호 불균형 SQL 생성</h3>

<p>LLM이 가끔 <code class="language-plaintext highlighter-rouge">WHERE (조건;</code> 처럼 닫는 괄호가 없는 SQL을 생성했습니다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">clean_sql</span><span class="p">(</span><span class="n">sql</span><span class="p">):</span>
    <span class="c1"># 괄호 균형 보정
</span>    <span class="n">open_count</span> <span class="o">=</span> <span class="n">sql</span><span class="p">.</span><span class="n">count</span><span class="p">(</span><span class="s">'('</span><span class="p">)</span>
    <span class="n">close_count</span> <span class="o">=</span> <span class="n">sql</span><span class="p">.</span><span class="n">count</span><span class="p">(</span><span class="s">')'</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">open_count</span> <span class="o">&gt;</span> <span class="n">close_count</span><span class="p">:</span>
        <span class="n">sql</span> <span class="o">=</span> <span class="n">sql</span><span class="p">.</span><span class="n">rstrip</span><span class="p">(</span><span class="s">';'</span><span class="p">)</span> <span class="o">+</span> <span class="s">')'</span> <span class="o">*</span> <span class="p">(</span><span class="n">open_count</span> <span class="o">-</span> <span class="n">close_count</span><span class="p">)</span> <span class="o">+</span> <span class="s">';'</span>
    <span class="k">return</span> <span class="n">sql</span>
</code></pre></div></div>

<hr />

<h2 id="결과">결과</h2>

<ul>
  <li>Streamlit Cloud 배포 및 실제 서비스 운영 중</li>
  <li>자연어로 통계 질문과 후기 질문을 모두 처리</li>
  <li>시즌별 통계 자동 계산으로 Google Sheets 구조 단순화</li>
</ul>

<hr />

<h2 id="회고">회고</h2>

<p><strong>잘한 점</strong></p>
<ul>
  <li>기획부터 배포까지 end-to-end 단독 수행</li>
  <li>실제 사용하는 데이터로 실용적인 서비스 구축</li>
  <li>버그를 만날 때마다 프롬프트 규칙과 코드 방어 로직 양쪽에서 접근</li>
</ul>

<p><strong>아쉬운 점 / 개선 계획</strong></p>
<ul>
  <li>LangSmith 연동으로 응답 품질 모니터링 추가 예정</li>
  <li>Airflow DAGs로 DB 자동 동기화 파이프라인 구축 예정 (LLMOps)</li>
  <li>GraphRAG 도입으로 선수 간 관계 분석 고도화 검토 중</li>
</ul>

<hr />

<h2 id="링크">링크</h2>

<ul>
  <li><strong>Live Demo</strong>: <a href="https://bm-stats-chatbot.streamlit.app">https://bm-stats-chatbot.streamlit.app</a></li>
  <li><strong>GitHub</strong>: <a href="https://github.com/insu97/badminton-stats-chatbot">https://github.com/insu97/badminton-stats-chatbot</a></li>
</ul>]]></content><author><name>INSU</name></author><category term="Project" /><category term="RAG" /><summary type="html"><![CDATA[배드민턴 통계 챗봇 개발기 - 2026년03월05일 tag : Project | RAG |]]></summary></entry><entry><title type="html">SQL</title><link href="http://localhost:4000/2025/12/13/SQL.html" rel="alternate" type="text/html" title="SQL" /><published>2025-12-13T00:00:00+09:00</published><updated>2025-12-13T00:00:00+09:00</updated><id>http://localhost:4000/2025/12/13/SQL</id><content type="html" xml:base="http://localhost:4000/2025/12/13/SQL.html"><![CDATA[<h1 id="sql---2025년12월13일">SQL - 2025년12월13일</h1>

<!-- - tag : SQL  -->
<ul>
  <li>tag : <a href=" /tags/tag_SQL.html" class="btn btn-default navbar-btn cursorNorm" role="button">SQL</a>
|</li>
</ul>

<hr />

<h2 id="sql">SQL</h2>

<h3 id="sql-injection">SQL Injection</h3>

<h4 id="개념-및-정의">개념 및 정의</h4>

<p>SQL Injection은 악의적인 사용자가 SQL 쿼리문에 비정상적인 SQL 코드를 삽입하여 데이터베이스를 공격하는 보안 취약점입니다. 웹 애플리케이션의 입력 필드나 URL 매개변수 등을 통해 악의적인 SQL 명령어를 주입하고, 이를 데이터베이스에서 실행하게 함으로써 권한 없이 데이터에 접근하거나 조작할 수 있습니다.</p>

<h4 id="문제점">문제점</h4>

<ul>
  <li><strong>데이터 유출</strong>: 데이터베이스의 민감한 정보(사용자 계정, 개인정보 등)가 노출될 수 있습니다.</li>
  <li><strong>데이터 손상</strong>: 악의적으로 데이터를 삭제하거나 수정할 수 있습니다.</li>
  <li><strong>인증 우회</strong>: 로그인 인증을 우회하여 관리자 권한으로 접근할 수 있습니다.</li>
  <li><strong>시스템 제어</strong>: 데이터베이스 서버에 대한 제어권을 얻을 수 있습니다.</li>
</ul>

<p><strong>SQL Injection 예시:</strong></p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- 정상적인 쿼리</span>
<span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">users</span> <span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="s1">'user_input'</span> <span class="k">AND</span> <span class="n">password</span> <span class="o">=</span> <span class="s1">'password_input'</span>

<span class="c1">-- 악의적인 입력 (id: ' OR '1'='1)</span>
<span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">users</span> <span class="k">WHERE</span> <span class="n">id</span> <span class="o">=</span> <span class="s1">''</span> <span class="k">OR</span> <span class="s1">'1'</span><span class="o">=</span><span class="s1">'1'</span> <span class="k">AND</span> <span class="n">password</span> <span class="o">=</span> <span class="s1">'password_input'</span>
<span class="c1">-- 결과: 모든 사용자 정보가 반환됨</span>
</code></pre></div></div>

<h4 id="예방-방법">예방 방법</h4>

<ol>
  <li><strong>Prepared Statement 사용</strong>
    <ul>
      <li>쿼리의 구조와 데이터를 분리하여 입력값을 매개변수로 처리합니다.
        <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- 안전한 방법 (Prepared Statement)</span>
<span class="k">PREPARE</span> <span class="n">stmt</span> <span class="k">FROM</span> <span class="s1">'SELECT * FROM users WHERE id = ? AND password = ?'</span>
<span class="k">EXECUTE</span> <span class="n">stmt</span> <span class="k">USING</span> <span class="n">user_input</span><span class="p">,</span> <span class="n">password_input</span>
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
  <li><strong>입력값 검증 및 필터링</strong>
    <ul>
      <li>사용자 입력값의 길이, 형식, 타입을 검증합니다.</li>
      <li>특수문자(‘ “ ; – /* */ 등)를 제거하거나 이스케이프 처리합니다.</li>
    </ul>
  </li>
  <li><strong>이스케이프 처리</strong>
    <ul>
      <li>쿼리에 포함되는 모든 문자열을 적절히 이스케이프 처리합니다.
        <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Python 예시
</span><span class="n">escaped_input</span> <span class="o">=</span> <span class="n">input_value</span><span class="p">.</span><span class="n">replace</span><span class="p">(</span><span class="s">"'"</span><span class="p">,</span> <span class="s">"''"</span><span class="p">)</span>
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
  <li><strong>최소 권한 원칙 (Least Privilege)</strong>
    <ul>
      <li>데이터베이스 계정에 필요한 최소한의 권한만 부여합니다.</li>
      <li>읽기 전용 계정, 특정 테이블만 접근 가능한 계정 등으로 제한합니다.</li>
    </ul>
  </li>
  <li><strong>에러 메시지 제한</strong>
    <ul>
      <li>데이터베이스 에러 메시지를 사용자에게 노출하지 않습니다.</li>
      <li>일반적인 에러 메시지만 반환하도록 처리합니다.</li>
    </ul>
  </li>
  <li><strong>웹 애플리케이션 방화벽 (WAF)</strong>
    <ul>
      <li>SQL Injection 패턴을 탐지하고 차단하는 WAF 솔루션을 사용합니다.</li>
    </ul>
  </li>
  <li><strong>정기적인 보안 감시 및 로깅</strong>
    <ul>
      <li>의심스러운 SQL 쿼리를 모니터링하고 로깅합니다.</li>
      <li>정기적인 보안 감사를 수행합니다.</li>
    </ul>
  </li>
</ol>]]></content><author><name>INSU</name></author><category term="SQL" /><summary type="html"><![CDATA[SQL - 2025년12월13일 tag : SQL |]]></summary></entry><entry><title type="html">Github</title><link href="http://localhost:4000/2025/12/13/Github.html" rel="alternate" type="text/html" title="Github" /><published>2025-12-13T00:00:00+09:00</published><updated>2025-12-13T00:00:00+09:00</updated><id>http://localhost:4000/2025/12/13/Github</id><content type="html" xml:base="http://localhost:4000/2025/12/13/Github.html"><![CDATA[<h1 id="github---2025년12월13일">Github - 2025년12월13일</h1>

<!-- - tag : Github  -->
<ul>
  <li>tag : <a href=" /tags/tag_Github.html" class="btn btn-default navbar-btn cursorNorm" role="button">Github</a>
|</li>
</ul>

<hr />

<h1 id="github">Github</h1>

<h2 id="초기-설정">초기 설정</h2>

<h3 id="환경설정">환경설정</h3>

<p><strong>정보 확인하기</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--list</span>
</code></pre></div></div>

<p><strong>이름과 이메일 설정</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> user.name <span class="s2">"사용자이름"</span>
git config <span class="nt">--global</span> user.email <span class="s2">"이메일주소"</span>
git config <span class="nt">--global</span> core.editor <span class="s2">"vim"</span>
git config <span class="nt">--global</span> core.pager <span class="s2">"cat"</span>
</code></pre></div></div>

<p><strong>Tip</strong></p>

<p>만약 <code class="language-plaintext highlighter-rouge">core.editor</code> 값이 vim, notepad 두 개가 있을 경우:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vi ~/.gitconfig
</code></pre></div></div>

<p>위 명령어 입력 후 notepad 부분을 지우고 저장 후 다시 <code class="language-plaintext highlighter-rouge">git config --list</code>로 확인하기</p>

<h2 id="local-환경에서-작업">Local 환경에서 작업</h2>

<h3 id="git-init">git init</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>디렉터리이름  <span class="c"># 디렉터리 생성</span>
<span class="nb">cd </span>디렉터리이름     <span class="c"># 디렉터리로 이동</span>
git init          <span class="c"># Git 저장소 초기화</span>
</code></pre></div></div>

<h3 id="git-add-git-commit">git add, git commit</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vim 파일이름  <span class="c"># 파일 생성 및 편집 [ i : 입력모드, esc : 명령모드, :wq! : 저장 후 종료 ]</span>
<span class="nb">cat </span>파일이름  <span class="c"># 파일 내용 확인</span>
git status   <span class="c"># 현재 상태 확인 (자주 확인해주는 것이 좋다)</span>
</code></pre></div></div>

<p><strong>커밋하기</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 방법 1: 스테이징 + 커밋 분리</span>
git add 파일명.확장자  <span class="c"># 특정 파일 스테이징</span>
git add <span class="nb">.</span>             <span class="c"># 모든 변경사항 스테이징</span>
git commit            <span class="c"># 에디터에서 커밋 메시지 작성</span>
git commit <span class="nt">-m</span> <span class="s2">"커밋 메시지"</span>  <span class="c"># 인라인으로 커밋 메시지 작성</span>

<span class="c"># 방법 2: 수정된 파일 자동 스테이징 + 커밋</span>
git commit <span class="nt">-am</span> <span class="s2">"커밋 메시지"</span>

<span class="c"># 방법 3: 메시지 없이 (에디터에서 입력)</span>
git commit <span class="nt">-a</span>
</code></pre></div></div>

<h3 id="git-branch-git-checkout">git branch, git checkout</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch              <span class="c"># 브랜치 목록 확인</span>
git branch <span class="nt">-r</span>           <span class="c"># 원격 브랜치 목록 확인</span>
git branch <span class="nt">-a</span>           <span class="c"># 모든 브랜치 목록 확인</span>
git branch 브랜치이름     <span class="c"># 새 브랜치 생성</span>
git checkout 브랜치이름   <span class="c"># 브랜치 전환</span>
git switch 브랜치이름     <span class="c"># 브랜치 전환 (최신 방식)</span>
git branch <span class="nt">-d</span> 브랜치이름  <span class="c"># 브랜치 삭제</span>
git branch <span class="nt">-D</span> 브랜치이름  <span class="c"># 브랜치 강제 삭제</span>
</code></pre></div></div>

<h3 id="git-merge">git merge</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout master         <span class="c"># 메인 브랜치로 전환</span>
git merge 브랜치이름         <span class="c"># 브랜치 병합</span>
git merge 브랜치이름 <span class="nt">--no-ff</span> <span class="c"># fast-forward를 방지하고 병합 커밋 생성</span>
</code></pre></div></div>

<h3 id="git-log">git log</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log               <span class="c"># 커밋 로그 확인</span>
git log <span class="nt">--oneline</span>     <span class="c"># 한 줄 요약 로그 확인</span>
git log <span class="nt">--graph</span>       <span class="c"># 그래프 형태로 로그 확인</span>
git log <span class="nt">-1</span> <span class="nt">--stat</span>     <span class="c"># 가장 최근 커밋 로그 확인</span>
</code></pre></div></div>

<p><strong>보기 좋은 로그 출력 설정</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> alias.lg <span class="s2">"log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset' --abbrev-commit"</span>
</code></pre></div></div>

<h3 id="gitignore">.gitignore</h3>

<p>특정 파일이나 디렉터리를 Git에서 추적하지 않도록 설정</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">touch</span> .gitignore  <span class="c"># .gitignore 빈 파일 생성</span>
</code></pre></div></div>

<p><strong>참고 사이트</strong></p>
<ul>
  <li><a href="https://www.toptal.com/developers/gitignore/">gitignore.io</a></li>
  <li><a href="https://www.toptal.com/developers/gitignore/api/macos,windows,python,flask,vim,visualstudiocode,jupyternotebooks">gitignore API 예시</a></li>
</ul>

<h2 id="원격-저장소-연동">원격 저장소 연동</h2>

<h3 id="깃허브-저장소-생성-및-clone">깃허브 저장소 생성 및 Clone</h3>

<p><strong>저장소 생성 단계:</strong></p>
<ol>
  <li>깃허브 접속</li>
  <li>Create repository → Public 선택, License: MIT 선택</li>
  <li>주소 복사 후 Git Bash에서 다음 명령어 입력:</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone <span class="s2">"저장소 주소"</span>  <span class="c"># 원격 저장소 복제</span>
</code></pre></div></div>

<h3 id="원격-저장소-추가-및-연동">원격 저장소 추가 및 연동</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote                              <span class="c"># 원격 저장소 확인 (origin)</span>
git remote <span class="nt">-v</span>                           <span class="c"># 연결된 원격 저장소 상세 확인</span>
git remote add origin 원격저장소URL      <span class="c"># 로컬 저장소를 원격 저장소와 연결</span>
</code></pre></div></div>

<h3 id="push--pull">Push &amp; Pull</h3>

<p><strong>로컬 커밋을 원격 저장소에 업로드</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push origin main              <span class="c"># origin의 main 브랜치로 업로드</span>
git push origin <span class="nt">--all</span>             <span class="c"># 모든 브랜치를 원격 저장소에 업로드</span>
git push <span class="nt">-u</span> origin feature/이름   <span class="c"># 새로운 브랜치 최초 push (-u는 한 번만 사용)</span>
</code></pre></div></div>

<p><strong>원격 저장소의 변경 사항 가져오기</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git fetch origin              <span class="c"># 원격 저장소의 변경 사항을 로컬로 가져오기</span>
git pull origin main          <span class="c"># 원격 저장소의 main 브랜치 변경 사항을 가져오고 병합</span>
git merge FETCH_HEAD          <span class="c"># fetch한 내용 병합</span>
</code></pre></div></div>

<h3 id="github-personal-access-token">GitHub Personal Access Token</h3>

<p><strong>비밀 키 받기:</strong></p>
<ol>
  <li>오른쪽 맨 위 프로필 아이콘 클릭 → Settings 클릭</li>
  <li>왼쪽 사이드 바에서 Developer Settings 클릭</li>
  <li>Personal access tokens → Tokens (Classic) 클릭</li>
  <li>Generate new token → Generate new token (classic) 선택</li>
</ol>

<h2 id="git-고급-기능">Git 고급 기능</h2>

<h3 id="stash">stash</h3>

<p>작업 중인 변경사항을 임시로 저장</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash              <span class="c"># 현재 변경사항 임시 저장</span>
git stash list         <span class="c"># stash 목록 보기</span>
git stash pop          <span class="c"># 가장 최근 stash 불러오기</span>
git stash pop stash@<span class="o">{</span>번호<span class="o">}</span>  <span class="c"># 특정 stash 불러오기</span>
</code></pre></div></div>

<h3 id="undo--restore">undo &amp; restore</h3>

<p>변경사항 취소하기</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git restore 파일명.파일확장자  <span class="c"># 파일을 최종 커밋 상태로 되돌리기</span>
git checkout HEAD <span class="nt">--</span> 파일명    <span class="c"># 파일을 마지막 커밋 상태로 되돌림</span>
</code></pre></div></div>

<h3 id="unstaging">Unstaging</h3>

<p>Stage의 변경사항을 Working Directory로 내리기</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add 파일명.파일확장자
git reset HEAD 파일명.파일확장자  <span class="c"># HEAD: 마지막의, 최신의</span>
</code></pre></div></div>

<h3 id="커밋-수정하기-amend">커밋 수정하기 (amend)</h3>

<p>최근 커밋 메시지 수정하기</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">--amend</span>                <span class="c"># 에디터에서 메시지 수정</span>
git commit <span class="nt">--amend</span> <span class="nt">-m</span> <span class="s2">"새 메시지"</span>  <span class="c"># 인라인으로 메시지 수정</span>
</code></pre></div></div>

<p>같은 브랜치 상에 있는 최종 커밋을 취소하고 새로운 내용을 추가하거나 설명을 덧붙인 커밋을 할 수 있습니다.</p>

<h3 id="revert-commit">Revert commit</h3>

<p>잘못을 인정하고 특정 시점으로 되돌리기 (이미 push된 커밋을 안전하게 취소)</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git revert <span class="nt">--no-commit</span> HEAD~되돌리고싶은커밋숫자..
git status    <span class="c"># 삭제 이력 확인</span>
git commit    <span class="c"># 왜 revert했는지 이유를 적어야 함</span>
</code></pre></div></div>

<p>또는</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git revert 커밋해시  <span class="c"># 특정 커밋을 취소하는 새로운 커밋 생성</span>
</code></pre></div></div>

<h3 id="reset">Reset</h3>

<p>어떤 특정 커밋을 사용하지 않게 되어 다시 되돌릴 때 사용<br />
<code class="language-plaintext highlighter-rouge">git revert</code>와 다르게 커밋 기록이 남지 않습니다.</p>

<p><strong>git reset 모드:</strong></p>

<table>
  <thead>
    <tr>
      <th>모드</th>
      <th>의미</th>
      <th>HEAD 위치</th>
      <th>인덱스</th>
      <th>작업 디렉터리</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>hard</td>
      <td>완전히 되돌림</td>
      <td>변경</td>
      <td>변경</td>
      <td>변경</td>
    </tr>
    <tr>
      <td>mixed</td>
      <td>인덱스의 상태를 되돌림 (기본값)</td>
      <td>변경</td>
      <td>변경</td>
      <td>유지</td>
    </tr>
    <tr>
      <td>soft</td>
      <td>커밋만 되돌림</td>
      <td>변경</td>
      <td>유지</td>
      <td>유지</td>
    </tr>
  </tbody>
</table>

<p><strong>git reset 옵션:</strong></p>

<table>
  <thead>
    <tr>
      <th>옵션</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>^ 혹은 ~</td>
      <td>~은 커밋 내역 하나를 의미. 표시한 수만큼 커밋을 되돌림</td>
    </tr>
    <tr>
      <td>ORIG_HEAD</td>
      <td>git reset 명령으로 지운 커밋 내역을 보관. 이를 통해 reset한 커밋을 되돌릴 수 있음</td>
    </tr>
  </tbody>
</table>

<h3 id="git-rebase">git rebase</h3>

<p>브랜치 이력을 정리하고 깔끔하게 병합</p>

<p><strong>기본 rebase:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout hotfix1
git rebase master     <span class="c"># master를 기준으로 hotfix1을 재배치</span>
<span class="c"># 충돌 발생 시:</span>
<span class="c"># 1. 충돌 해결</span>
<span class="c"># 2. git add 파일명</span>
<span class="c"># 3. git rebase --continue</span>

git checkout master
git merge hotfix1 <span class="nt">--no-ff</span>  <span class="c"># master로 병합</span>
</code></pre></div></div>

<p><strong>rebase 옵션:</strong></p>

<table>
  <thead>
    <tr>
      <th>옵션</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>–continue</td>
      <td>충돌이 해결된 후 rebase를 계속 진행</td>
    </tr>
    <tr>
      <td>–abort</td>
      <td>rebase를 중단하고 이전 상태로 되돌림</td>
    </tr>
    <tr>
      <td>–skip</td>
      <td>충돌이 발생한 커밋을 건너뛰고 rebase를 계속 진행</td>
    </tr>
  </tbody>
</table>

<p><strong>인터랙티브 rebase (커밋 합치기):</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git rebase <span class="nt">-i</span> HEAD~~  <span class="c"># 서로 다른 두 개의 커밋 내역을 합침 (~ : 커밋 내역 하나)</span>
</code></pre></div></div>

<h2 id="merge-conflict-해결">Merge Conflict 해결</h2>

<h3 id="1-일반적인-상황-merge">1. 일반적인 상황 (Merge)</h3>

<p>Merge 상황에서 충돌 해결</p>

<h3 id="2-특수한-상황-rebase">2. 특수한 상황 (Rebase)</h3>

<p>rebase 방법으로 충돌 해결</p>

<h2 id="branch-models">Branch Models</h2>

<h3 id="git-flow">git flow</h3>

<ul>
  <li>가장 전통적이고 많이 쓰이는 모델</li>
  <li>각 단계가 명확히 구분되어 배포 주기가 주기적인 서비스에 유리</li>
  <li>단점: 복잡함</li>
</ul>

<h3 id="github-flow">github flow</h3>

<ul>
  <li>브랜치 모델의 단순화</li>
  <li>CI 의존성이 높고, pull request가 없으면 실수에 대처가 힘듦</li>
</ul>

<p><strong>GitHub Flow 작업 순서:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. 새 브랜치 생성 및 전환</span>
git branch feature/기능이름
git switch feature/기능이름

<span class="c"># 2. 작업 및 커밋</span>
vi 파일명.확장자  <span class="c"># main branch와 다르게 수정</span>
git add 파일명.확장자
git commit

<span class="c"># 3. 원격에 push</span>
git push <span class="nt">-u</span> origin feature/기능이름  <span class="c"># -u는 한 번만</span>

<span class="c"># 4. GitHub에서 Pull Request 생성</span>
<span class="c"># - Pull requests → Create pull request</span>
<span class="c"># - feature/기능이름 선택</span>
<span class="c"># - Title, description 작성 + Assignees(담당자) 선택</span>
<span class="c"># - Create pull request → Merge pull request → Confirm merge</span>

<span class="c"># 5. 브랜치 삭제 (GitHub 웹에서)</span>
<span class="c"># - main 클릭 → View all branches → feature/기능이름 삭제</span>

<span class="c"># 6. 로컬 브랜치 삭제</span>
git branch <span class="nt">-D</span> feature/기능이름

<span class="c"># 7. 최종 데이터 pull</span>
git pull origin main
</code></pre></div></div>

<h3 id="gitlab-flow">gitlab flow</h3>

<p>Deploy, issue에 대응하기 쉽도록 한 모델</p>

<h2 id="github-협업-도구">GitHub 협업 도구</h2>

<h3 id="pre-commit">pre-commit</h3>

<p>commit 수행 전 체크해야 할 것들을 자동 수행하도록 도와주는 도구</p>

<p><strong>설치 및 설정:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>pre-commit              <span class="c"># 설치</span>
pre-commit <span class="nt">--version</span>                <span class="c"># 설치 확인</span>
pre-commit sample-config <span class="o">&gt;</span> .pre-commit-config.yaml  <span class="c"># 설정 파일 생성</span>
<span class="nb">cat</span> .pre-commit-config.yaml         <span class="c"># 내용 보기</span>
</code></pre></div></div>

<p><strong>사용 방법 (commit 하기 전):</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pre-commit run      <span class="c"># 스테이징된 파일 체크</span>
pre-commit run <span class="nt">-a</span>   <span class="c"># 모든 파일 체크 (주로 사용)</span>
</code></pre></div></div>

<p><strong>참고 사이트:</strong> <a href="https://pre-commit.com/">pre-commit.com</a></p>

<h3 id="issue">Issue</h3>

<p>GitHub 프로젝트의 다양한 이슈를 관리하기 위한 기능</p>

<ul>
  <li><strong>용도:</strong> 할 일, 버그, 질문 등을 관리</li>
  <li><strong>중요:</strong> Label, 상태 관리 등의 업데이트가 잘 이루어져야 원활한 작업 가능</li>
  <li><strong>Template:</strong> GitHub repository → Settings → Issue template에서 설정</li>
  <li><strong>Issue Labels:</strong> 상태와 종류, 긴급도 등을 표시 (<a href="https://medium.com/@dave_lunny/sane-github-labels-c5d2e6004b63">참고</a>)</li>
  <li><strong>Milestone:</strong> Issue가 프로젝트의 어떤 주기에서 해결되어야 하는지 표기. Sprint의 달성률과 남은 Issue 파악이 쉬워짐</li>
</ul>

<h3 id="projects">Projects</h3>

<p>GitHub에서 repository issue 기반의 task management</p>

<ul>
  <li>Scrum board와 table 방식 존재</li>
  <li>팀의 Admin만 관리 가능</li>
  <li>commit, pull request 등을 통해 자동으로 움직이도록 관리 가능</li>
</ul>

<h3 id="wiki">Wiki</h3>

<p>Repository에서 README.md보다 더 자세한 설명이 필요할 때 작성</p>

<ul>
  <li>별도 사이트 없이도 FAQ, Docs 처리 가능</li>
  <li>프로젝트 중 문서화가 필요한 모든 것들을 담아놓는 공간</li>
  <li><strong>포함 내용:</strong> Daily Scrum, Sprint Retrospection (Liked, Learned, Lacked), Code Convention, Technical Issues</li>
</ul>

<h3 id="pull-request">Pull Request</h3>

<p><strong>명령어 지원:</strong></p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">close</code>, <code class="language-plaintext highlighter-rouge">resolve</code>, <code class="language-plaintext highlighter-rouge">fix</code> (복수형, 과거형)</li>
  <li>예: <code class="language-plaintext highlighter-rouge">resolve #1</code></li>
</ul>

<h2 id="팀-프로젝트-협업">팀 프로젝트 협업</h2>

<h3 id="팀-project-remote-과정-main-branch에서-실행">팀 Project REMOTE 과정 (main branch에서 실행)</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git remote <span class="nt">-v</span>                              <span class="c"># remote 확인</span>
git remote add upstream 팀리포지토리주소     <span class="c"># 관습적으로 팀 이름은 upstream</span>
git fetch upstream main                    <span class="c"># FETCH_HEAD 공간에 쌓임</span>
git merge FETCH_HEAD
git push origin main
</code></pre></div></div>

<h2 id="커밋-메시지-작성-규칙">커밋 메시지 작성 규칙</h2>

<p><strong>Conventional Commits 지키기!</strong></p>

<p>참고 사이트: <a href="https://www.conventionalcommits.org/ko/v1.0.0/">conventionalcommits.org</a></p>

<h2 id="유용한-리소스">유용한 리소스</h2>

<ol>
  <li>
    <p><strong>GitHub Repository 생성 사이트</strong><br />
<a href="https://rahuldkjain.github.io/gh-profile-readme-generator/">gh-profile-readme-generator</a></p>
  </li>
  <li><strong>gitignore 파일 만들기</strong>
    <ul>
      <li><a href="https://www.toptal.com/developers/gitignore/">gitignore.io</a></li>
      <li><a href="https://www.toptal.com/developers/gitignore/api/macos,windows,python,flask,vim,visualstudiocode,jupyternotebooks">API 예시</a></li>
    </ul>
  </li>
  <li><strong>GitHub에서 Jupyter Notebook 변경사항 보기</strong><br />
오른쪽 위 프로필 사진 클릭 → Feature preview → Rich Jupyter Notebook Diffs 활성화</li>
</ol>

<h2 id="git-사용-시-주의사항">Git 사용 시 주의사항</h2>

<h3 id="습관들이면-안-되는-것들">습관들이면 안 되는 것들</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ git add .</span>
<span class="c"># 현재 디렉토리 하위의 모든 파일을 staging할 때 사용</span>
<span class="c"># 문제: 현재 commit 단위에 들어가면 안 되는 파일까지 휩쓸릴 수 있음</span>

<span class="c"># ❌ git commit -m "Message"</span>
<span class="c"># 셸에서 바로 메시지를 쓰면서 커밋</span>
<span class="c"># 문제: revert commit, merge commit 등의 상황에서 메시지를 덮어쓸 우려</span>

<span class="c"># ❌ 저장소 안에 저장소 clone 하기</span>
<span class="c"># 프로젝트 단위는 항상 독립적으로 존재해야 함</span>

<span class="c"># ❌ 의미없는 commit message 남기기 (예: a, 1, ..)</span>
<span class="c"># commit message는 제목만으로 해당 작업 단위에 대한 설명이 가능해야 함</span>
</code></pre></div></div>

<h3 id="반드시-지켜야-할-것">반드시 지켜야 할 것</h3>

<ul>
  <li><strong>Conventional Commit 잘 지키기!</strong></li>
  <li>의미 있는 commit 단위로 작업하기</li>
  <li>커밋 메시지는 명확하고 구체적으로 작성하기</li>
</ul>

<h2 id="실습-과제">실습 과제</h2>

<p>직접 프로젝트를 생성하고 협업하는 과정을 처음부터 끝까지 해보기!</p>]]></content><author><name>INSU</name></author><category term="Github" /><summary type="html"><![CDATA[Github - 2025년12월13일 tag : Github |]]></summary></entry><entry><title type="html">Anaconda</title><link href="http://localhost:4000/2025/09/20/anaconda_settings.html" rel="alternate" type="text/html" title="Anaconda" /><published>2025-09-20T00:00:00+09:00</published><updated>2025-09-20T00:00:00+09:00</updated><id>http://localhost:4000/2025/09/20/anaconda_settings</id><content type="html" xml:base="http://localhost:4000/2025/09/20/anaconda_settings.html"><![CDATA[<h1 id="anaconda---2025년09월20일">Anaconda - 2025년09월20일</h1>

<!-- - tag : Anaconda  -->
<ul>
  <li>tag : <a href=" /tags/tag_Anaconda.html" class="btn btn-default navbar-btn cursorNorm" role="button">Anaconda</a>
|</li>
</ul>

<hr />

<h2 id="anaconda-설치-후-환경-변수-설정">Anaconda 설치 후 환경 변수 설정</h2>

<ol>
  <li>환경 변수 진입</li>
  <li>Path 클릭</li>
  <li>경로 추가
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    anaconda3
    anaconda3\Library
    anaconda3\Scripts
</code></pre></div>    </div>
  </li>
</ol>]]></content><author><name>INSU</name></author><category term="Anaconda" /><summary type="html"><![CDATA[Anaconda - 2025년09월20일 tag : Anaconda |]]></summary></entry><entry><title type="html">Jekyll Blog</title><link href="http://localhost:4000/2025/07/25/Jekyll_Blog.html" rel="alternate" type="text/html" title="Jekyll Blog" /><published>2025-07-25T00:00:00+09:00</published><updated>2025-07-25T00:00:00+09:00</updated><id>http://localhost:4000/2025/07/25/Jekyll_Blog</id><content type="html" xml:base="http://localhost:4000/2025/07/25/Jekyll_Blog.html"><![CDATA[<h1 id="jekyll-blog---2025년07월25일">Jekyll Blog - 2025년07월25일</h1>

<!-- - tag : Jekyll_Blog  -->
<ul>
  <li>tag : <a href=" /tags/tag_Jekyll_Blog.html" class="btn btn-default navbar-btn cursorNorm" role="button">Jekyll_Blog</a>
|</li>
</ul>

<hr />

<h2 id="settings">Settings</h2>

<h3 id="새로운-환경-설정">새로운 환경 설정</h3>
<ul>
  <li>참고: <a href="https://jekyllrb-ko.github.io/tutorials/using-jekyll-with-bundler/">Jekyll with Bundler</a></li>
</ul>

<h4 id="1-ruby-설치">1) Ruby 설치</h4>
<ul>
  <li>참고: <a href="https://rubyinstaller.org/">Ruby Installer</a></li>
  <li>현재 사용 버전: 3.2.9</li>
</ul>

<h4 id="2-bundler-설치-및-사용">2) Bundler 설치 및 사용</h4>
<ul>
  <li>
    <p>참고: <a href="https://bundler.io/">Bundler</a></p>
  </li>
  <li>Gemfile이 있는 경우:
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bundle <span class="nb">install</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p>Gemfile이 없는 경우:</p>

    <p>1) Gemfile 작성</p>
    <div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">source</span> <span class="s1">'https://rubygems.org'</span>
<span class="n">gem</span> <span class="s1">'nokogiri'</span>
<span class="n">gem</span> <span class="s1">'rack'</span><span class="p">,</span> <span class="s1">'~&gt; 2.2.4'</span>
<span class="n">gem</span> <span class="s1">'rspec'</span>
</code></pre></div>    </div>
    <p>2) 설치 실행</p>
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bundle <span class="nb">install</span>
</code></pre></div>    </div>
  </li>
</ul>

<h2 id="error">ERROR</h2>

<h3 id="ruby--bundler-호환성-문제">Ruby ↔ Bundler 호환성 문제</h3>
<p>에러 예시:</p>
<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>C:/Ruby34-x64/... NameError: uninitialized constant DidYouMean::SPELL_CHECKERS
</code></pre></div></div>
<p>해결:</p>
<ul>
  <li>Ruby 버전을 호환되는 버전으로 조정</li>
  <li>Bundler 재설치 및 다시 시도</li>
</ul>

<h3 id="jekyll-build-error">Jekyll Build Error</h3>
<p>에러 예시:</p>
<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Invalid date '&lt;%= Time.now.strftime('%Y-%m-%d %H:%M:%S %z') %&gt;': ... does not have a valid date in the YAML front matter.
</code></pre></div></div>
<p>해결:</p>
<ul>
  <li>GitHub Actions 환경에서 <code class="language-plaintext highlighter-rouge">vendor/</code> 내부 템플릿이 빌드에 포함되지 않도록 설정
    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># _config.yml</span>
<span class="na">exclude</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">vendor</span>
</code></pre></div>    </div>
  </li>
</ul>

<h2 id="seo-sitemapxml-설정-가이드">SEO: Sitemap.xml 설정 가이드</h2>

<h3 id="개요">개요</h3>
<ul>
  <li>검색 엔진이 사이트 구조를 빠르게 파악하고 색인하는 데 사용하는 XML 파일입니다.</li>
</ul>

<h3 id="설정-절차-3단계">설정 절차 (3단계)</h3>
<ol>
  <li>Gem 추가
    <div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Gemfile</span>
<span class="n">gem</span> <span class="s1">'jekyll-sitemap'</span>
</code></pre></div>    </div>
  </li>
  <li>Jekyll 설정 업데이트
    <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># _config.yml</span>
<span class="na">url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://insu97.github.io"</span>   <span class="c1"># 자신의 GitHub Pages 도메인</span>
<span class="na">plugins</span><span class="pi">:</span>
     <span class="pi">-</span> <span class="s">jekyll-sitemap</span>
</code></pre></div>    </div>
  </li>
  <li>robots.txt 추가(권장)
    <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code># robots.txt (루트에 생성)
User-agent: *
Allow: /
Sitemap: https://insu97.github.io/sitemap.xml
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="확인-방법">확인 방법</h3>
<ul>
  <li>로컬
    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bundle <span class="nb">install
</span>bundle <span class="nb">exec </span>jekyll serve
<span class="c"># http://localhost:4000/sitemap.xml 에서 확인</span>
</code></pre></div>    </div>
  </li>
  <li>배포 후
      - <code class="language-plaintext highlighter-rouge">https://insu97.github.io/sitemap.xml</code> 접속
      - Google Search Console → Sitemaps에 제출</li>
</ul>

<h3 id="주의사항">주의사항</h3>
<ul>
  <li><code class="language-plaintext highlighter-rouge">_config.yml</code>의 <code class="language-plaintext highlighter-rouge">url</code>은 실제 도메인으로 정확히 설정합니다.</li>
  <li><code class="language-plaintext highlighter-rouge">plugins</code>에 <code class="language-plaintext highlighter-rouge">jekyll-sitemap</code>이 포함되어 있어야 자동 생성됩니다.</li>
</ul>]]></content><author><name>INSU</name></author><category term="Jekyll_Blog" /><summary type="html"><![CDATA[Jekyll Blog - 2025년07월25일 tag : Jekyll_Blog |]]></summary></entry><entry><title type="html">Runpod</title><link href="http://localhost:4000/2025/07/25/RUNPOD.html" rel="alternate" type="text/html" title="Runpod" /><published>2025-07-25T00:00:00+09:00</published><updated>2025-07-25T00:00:00+09:00</updated><id>http://localhost:4000/2025/07/25/RUNPOD</id><content type="html" xml:base="http://localhost:4000/2025/07/25/RUNPOD.html"><![CDATA[<h1 id="runpod---2025년07월25일">Runpod - 2025년07월25일</h1>

<!-- - tag : RUNPOD  -->
<ul>
  <li>tag : <a href=" /tags/tag_RUNPOD.html" class="btn btn-default navbar-btn cursorNorm" role="button">RUNPOD</a>
|</li>
</ul>

<hr />

<h2 id="error">ERROR</h2>

<h3 id="runpod에-vscode로-접속-시">runpod에 vscode로 접속 시</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">install </span>terminal quit with output: 프로세스에서 없는 파이프에 쓰려고 했습니다.
</code></pre></div></div>

<blockquote>
  <p>해결책 : C:\Users\{사용자이름}\.ssh 접속 후 known_hosts 파일 열어서 전부 삭제</p>
</blockquote>

<h2 id="others">Others</h2>

<h3 id="runpod-files--local-downloads">RUNPOD files → Local Downloads</h3>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># powershell</span><span class="w">
</span><span class="n">scp</span><span class="w"> </span><span class="nt">-r</span><span class="w"> </span><span class="nt">-P</span><span class="w"> </span><span class="p">{</span><span class="err">포트번호</span><span class="p">}</span><span class="w"> </span><span class="err">`</span><span class="w">
  </span><span class="nt">-i</span><span class="w"> </span><span class="s2">"C:\Users\YourUser\.ssh\id_ed25519"</span><span class="w"> </span><span class="err">`</span><span class="w">
  </span><span class="s2">"root@{서버주소}:{원격폴더경로}"</span><span class="w"> </span><span class="err">`</span><span class="w">
  </span><span class="s2">"{로컬 저장 경로}"</span><span class="w">
</span></code></pre></div></div>]]></content><author><name>INSU</name></author><category term="RUNPOD" /><summary type="html"><![CDATA[Runpod - 2025년07월25일 tag : RUNPOD |]]></summary></entry></feed>